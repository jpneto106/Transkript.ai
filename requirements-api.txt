# Dependências adicionais do aplicativo desktop (API + janela nativa).
# Instale na mesma venv do projeto:  venv\Scripts\pip.exe install -r requirements-api.txt
fastapi>=0.110
uvicorn[standard]>=0.30
websockets>=12.0
requests>=2.31
python-multipart>=0.0.9
# pywebview não é mais usado (a interface abre em janela do Edge --app), mas pode
# ficar instalado sem problema.
# Opcionais para os formatos de saída extras (v4-leve):
#   python-docx: gera .docx (Word). Sem ele, pedir 'docx' em escrever_saidas levanta erro.
#   fpdf2:       gera .pdf. Sem ele, idem.
python-docx>=1.1.0
fpdf2>=2.7.9
# Motor de transcrição NVIDIA (Parakeet/Canary) via onnx-asr. Sem este pacote,
# pedir um desses modelos dá erro de import. CPU-only por padrão; com as
# libs CUDA abaixo e ``onnxruntime-gpu`` no lugar de ``onnxruntime``, usa GPU.
onnx-asr>=0.7
# onnxruntime com CUDA EP embutido (versão GPU). Sem GPU instalada, ainda
# funciona — cai para CPU EP automaticamente, só perde a aceleração. Esta
# versão inclui o CPU EP também, então substitui a ``onnxruntime`` padrão
# sem quebrar o Whisper.
onnxruntime-gpu>=1.18
# CUDA 12 para os modelos NVIDIA Parakeet/Canary. Sem essas duas o
# onnxruntime-gpu dá "Library cublas64_12.dll is not found" ao usar Parakeet.
# (Sem GPU, esses pacotes ficam instalados mas inativos — o motor cai
# para CPU sozinho.)
nvidia-cublas-cu12
nvidia-cudnn-cu12
