"""Dependências OPCIONAIS do Transkript.ai v4.

Quem usa só o Whisper (CPU ou GPU) não precisa instalar nada daqui.

Quem usa **identificação de falantes (diarização)**, sim. O pyannote.audio
traz o PyTorch junto (~2,5 GB). Instale com GPU NVIDIA para ter velocidade real
(sem GPU a diarização leva 5-10 minutos mesmo em áudios curtos):

    pip install pyannote.audio>=3.1
    pip install --upgrade --force-reinstall torch torchaudio --index-url https://download.pytorch.org/whl/cu124

Sem GPU NVIDIA:

    pip install pyannote.audio>=3.1

O modelo (``pyannote/speaker-diarization-community-1``, 32 MB) é baixado
uma única vez e guardado em ``<app>/modelos/hub/`` — sem precisar de conta
no Hugging Face.
"""

pyannote.audio>=3.1
# PyTorch CUDA: se você tem GPU NVIDIA, rode o comando acima DEPOIS de
# pip install -r requirements-extras.txt (o pip puxa PyTorch CPU por padrão).
# torch>=2.6  -- explicitado no comando acima com --index-url cu124, não aqui.
