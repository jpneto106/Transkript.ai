# Dependências adicionais do aplicativo desktop (API + janela nativa).
# Instale na mesma venv do projeto:  venv\Scripts\pip.exe install -r requirements-api.txt
fastapi>=0.110
uvicorn[standard]>=0.30
websockets>=12.0
requests>=2.31
python-multipart>=0.0.9
# pywebview não é mais usado (a interface abre em janela do Edge --app), mas pode
# ficar instalado sem problema.
# Opcionais para os formatos de saída extras (v4-leve):
#   python-docx: gera .docx (Word). Sem ele, pedir 'docx' em escrever_saidas levanta erro.
#   fpdf2:       gera .pdf. Sem ele, idem.
python-docx>=1.1.0
fpdf2>=2.7.9
# Motor de transcrição NVIDIA (Parakeet/Canary) via onnx-asr.
# Usa onnxruntime padrão (CPU) para funcionar em qualquer máquina.
# Quem tem GPU NVIDIA pode trocar para onnxruntime-gpu depois.
onnx-asr>=0.7
# onnxruntime padrão — funciona em CPU e GPU (sem aceleração CUDA).
# Para GPU NVIDIA nos modelos Parakeet/Canary, instale onnxruntime-gpu.
onnxruntime
